Build Instructions
git clone --recursive https://github.com/Huang-lab/figure-extractor.git
cd figure-extractor
docker build -t figure-extractor .

then run the start_parser.sh  it will run the container 